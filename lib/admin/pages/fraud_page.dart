import 'package:flutter/material.dart';

class FraudPage extends StatelessWidget {
  const FraudPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Fraud Review'));
  }
}

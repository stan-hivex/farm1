import 'package:flutter/material.dart';

class MerchantsPage extends StatelessWidget {
  const MerchantsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Merchants'));
  }
}

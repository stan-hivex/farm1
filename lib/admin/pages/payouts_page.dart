import 'package:flutter/material.dart';

class PayoutsPage extends StatelessWidget {
  const PayoutsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Payouts'));
  }
}
